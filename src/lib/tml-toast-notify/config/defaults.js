// Configuración por defecto
const DEFAULTS = {
    duration: 5000,
    position: 'bottom-right',
    sound: true,
    theme: 'light' // Preparado para futuro soporte dark/light
};

module.exports = { DEFAULTS };
